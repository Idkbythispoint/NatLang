!notes[This password manager securely stores, retrieves, and deletes user passwords.]

!func name=addPassword[
    Adds a new password for a specified service.
]
!func name=getPassword[
    Retrieves the password for a specified service.
]
!func name=deletePassword[
    Deletes the password for a specified service.
]
!func name=main[
    the main code, with the CLI user interactions and menu
]