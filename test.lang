!func name=read_keys_from_file[
read the keys from the keys.txt file and return them as a list
]

!func name=check_api_key[
check if the provided API key is in the list of keys
]

!func name=api_key_check_endpoint[
define the Flask route that takes an API key as a parameter and returns a JSON response indicating if the key is valid
]